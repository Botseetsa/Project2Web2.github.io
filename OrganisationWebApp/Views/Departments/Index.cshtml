@model IEnumerable<OrganisationWebApp.Models.Department>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobR)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmpId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BusinessT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobR)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
